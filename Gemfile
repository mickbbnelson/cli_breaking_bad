# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in bb-cli.gemspec


gem "rake", "~> 13.0"
gem "pry"
gem "json"          #makes everything pretty
gem "rest-client"   #grabs info from API url and allows us to manipulate it
gem "require_all"    #allows us to put require_all folder instead of specific files in the environment file
gem "colorize"      #allows you to add color to a string